const state={money:Number(localStorage.getItem("dl_money")||12500),category:"wheels",selected:null,installed:JSON.parse(localStorage.getItem("dl_build")||"{}"),stats:{power:425,torque:890,weight:7150,grip:62,style:48}};
const parts={wheels:[{id:"factory-wheel",name:"Factory Wheel",price:0,note:"18 inch",style:0,wheel:"factory"},{id:"black-forged",name:"Black Forged",price:1400,note:"22x12",style:8,wheel:"black"},{id:"chrome-deep",name:"Chrome Deep Dish",price:1900,note:"24x14",style:13,wheel:"chrome"},{id:"show-polished",name:"Polished Show Wheel",price:2600,note:"26x16",style:20,wheel:"polished"}],tires:[{id:"street-tire",name:"Street Tire",price:0,note:"Factory grip",grip:0},{id:"all-terrain",name:"All Terrain",price:850,note:"+5 grip",grip:5},{id:"drag-radial",name:"Drag Radial",price:1200,note:"+12 grip",grip:12},{id:"mud-tire",name:"Mud Tire",price:1450,note:"Show + pull",grip:7,style:5}],lift:[{id:"stock-height",name:"Stock Height",price:0,note:"Factory",lift:0},{id:"level-2",name:'2" Level',price:650,note:"Front level",lift:2,style:3},{id:"lift-6",name:'6" Lift',price:1800,note:"Aggressive stance",lift:6,style:10},{id:"lift-10",name:'10" Show Lift',price:3600,note:"Massive stance",lift:10,style:18}],paint:[{id:"factory-paint",name:"Factory Paint",price:0,note:"Original",color:null},{id:"black-paint",name:"Gloss Black",price:1100,note:"Full respray",color:"#111111",style:5},{id:"red-paint",name:"Deep Red",price:1350,note:"Candy style",color:"#8b1111",style:9},{id:"blue-paint",name:"Midnight Blue",price:1350,note:"Metallic",color:"#123d8a",style:9}],lights:[{id:"stock-lights",name:"Stock Lighting",price:0,note:"Factory",glow:false},{id:"white-led",name:"LED Conversion",price:500,note:"+2 style",style:2},{id:"blue-underglow",name:"Blue Underglow",price:700,note:"Show lighting",glow:true,style:9}],bumper:[{id:"factory-bumper",name:"Factory Bumper",price:0,note:"Stock",style:0},{id:"painted-bumper",name:"Painted Bumper",price:900,note:"Color matched",style:7},{id:"offroad-bumper",name:"Heavy Offroad Bumper",price:1600,note:"+85 lb",weight:85,style:6}],performance:[{id:"stock-tune",name:"Stock Tune",price:0,note:"425 HP",power:0,torque:0},{id:"street-tune",name:"Street Tune",price:650,note:"+75 HP",power:75,torque:130},{id:"race-tune",name:"Race Tune",price:1700,note:"+170 HP",power:170,torque:290},{id:"big-turbo",name:"Big Turbo Setup",price:4200,note:"+300 HP",power:300,torque:460}]};
const el=id=>document.getElementById(id); el("money").textContent=state.money.toLocaleString();
document.querySelectorAll('.nav-btn').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.nav-btn').forEach(b=>b.classList.remove('active'));document.querySelectorAll('.screen').forEach(s=>s.classList.remove('active'));btn.classList.add('active');el(btn.dataset.screen).classList.add('active');}));
document.querySelectorAll('.category').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.category').forEach(b=>b.classList.remove('active'));btn.classList.add('active');state.category=btn.dataset.cat;renderParts();}));
function getInstalledPart(category){const id=state.installed[category];return parts[category]?.find(p=>p.id===id)||parts[category]?.[0]||null;}
function setWheelLook(wheelEls,wheelType){const show=wheelType&&wheelType!=="factory";wheelEls.forEach(w=>{w.style.opacity=show?"1":"0";if(wheelType==="black"){w.style.filter="brightness(.55)";}else if(wheelType==="chrome"){w.style.filter="brightness(1.4) contrast(1.15)";}else if(wheelType==="polished"){w.style.filter="brightness(1.8)";}else{w.style.filter="";}});}
function updateRaceTruckStyle(){const paint=getInstalledPart('paint');const lift=getInstalledPart('lift');const wheels=getInstalledPart('wheels');const lights=getInstalledPart('lights');const playerPaint=el('playerPaint');if(paint?.color){playerPaint.style.background=paint.color;playerPaint.style.opacity='.62';}else{playerPaint.style.opacity='0';} const liftValue=lift?.lift||0; const playerTruck=el('playerTruck'); playerTruck.dataset.raise=String(liftValue*1.8); playerTruck.dataset.scale=String(1+(liftValue*0.01)); setWheelLook([el('raceFrontWheel'),el('raceRearWheel')],wheels?.wheel); el('playerGlow').style.opacity=lights?.glow?'.85':'0';}
function applyVisual(category,part){if(category==='wheels'){setWheelLook([el('frontWheel'),el('rearWheel')],part.wheel);updateRaceTruckStyle();} if(category==='lift'){const scale=1+((part.lift||0)*0.012); const y=-((part.lift||0)*2.2); el('truckWrap').style.transform=`translateY(${y}px) scale(${scale})`; updateRaceTruckStyle();} if(category==='paint'){const tint=el('paintTint'); if(part.color){tint.style.background=part.color; tint.style.opacity='.62';} else {tint.style.opacity='0';} updateRaceTruckStyle();} if(category==='lights'){el('underglow').style.opacity=part.glow?'.85':'0'; updateRaceTruckStyle();}}
function renderParts(){const list=parts[state.category]; if(!state.selected||!list.some(p=>p.id===state.selected.id)) state.selected=list[0]; const strip=el('partsStrip'); strip.innerHTML=''; list.forEach(part=>{const card=document.createElement('div'); card.className='part-card'+(part.id===state.selected.id?' active':''); card.innerHTML=`<div class="name">${part.name}</div><div class="price">${part.price===0?'OWNED':'$'+part.price.toLocaleString()}</div><div class="mini">${part.note||''}</div>`; card.onclick=()=>{state.selected=part; renderParts(); showSelected(); applyVisual(state.category,part);}; strip.appendChild(card);}); showSelected();}
function showSelected(){if(!state.selected) return; el('selectedPart').textContent=state.selected.name; const installed=state.installed[state.category]===state.selected.id; el('selectedPrice').textContent=installed?'INSTALLED':state.selected.price===0?'OWNED':'$'+state.selected.price.toLocaleString(); el('buyBtn').textContent=installed?'INSTALLED':'INSTALL';}
function recalcStats(){let stats={power:425,torque:890,weight:7150,grip:62,style:48}; Object.entries(state.installed).forEach(([category,id])=>{const part=parts[category]?.find(p=>p.id===id); if(!part) return; stats.power+=part.power||0; stats.torque+=part.torque||0; stats.weight+=part.weight||0; stats.grip+=part.grip||0; stats.style+=part.style||0;}); state.stats=stats; el('powerStat').textContent=`${stats.power} HP`; el('torqueStat').textContent=`${stats.torque} LB-FT`; el('weightStat').textContent=`${stats.weight.toLocaleString()} LB`; el('gripStat').textContent=`${stats.grip}%`; el('styleStat').textContent=stats.style;}
el('buyBtn').addEventListener('click',()=>{const part=state.selected; if(!part) return; const already=state.installed[state.category]===part.id; if(already) return; if(part.price>state.money){alert('Not enough money.'); return;} if(part.price>0) state.money-=part.price; state.installed[state.category]=part.id; localStorage.setItem('dl_build',JSON.stringify(state.installed)); localStorage.setItem('dl_money',state.money); el('money').textContent=state.money.toLocaleString(); applyVisual(state.category,part); recalcStats(); renderParts();});
el('saveBtn').addEventListener('click',()=>{localStorage.setItem('dl_build',JSON.stringify(state.installed)); localStorage.setItem('dl_money',state.money); el('saveBtn').textContent='SAVED'; setTimeout(()=>el('saveBtn').textContent='SAVE BUILD',900);});
function loadInstalledVisuals(){Object.entries(state.installed).forEach(([category,id])=>{const part=parts[category]?.find(p=>p.id===id); if(part) applyVisual(category,part);}); updateRaceTruckStyle();}
renderParts(); loadInstalledVisuals(); recalcStats();
const race={active:false,started:false,finished:false,throttle:false,nitrousOn:false,gear:1,rpm:900,speed:0,distance:0,opponentDistance:0,opponentSpeed:0,nitrous:100,launchTime:0,lastTime:0,resultShown:false};
const treeIds=['pre1','pre2','amber1','amber2','amber3','greenBulb','redBulb']; const sleep=ms=>new Promise(r=>setTimeout(r,ms)); function clearTree(){treeIds.forEach(id=>el(id).classList.remove('on'));} function lightOn(id){el(id).classList.add('on');}
function resetRace(){race.active=false;race.started=false;race.finished=false;race.throttle=false;race.nitrousOn=false;race.gear=1;race.rpm=900;race.speed=0;race.distance=0;race.opponentDistance=0;race.opponentSpeed=0;race.nitrous=100;race.resultShown=false; clearTree(); updateHud(); updateRaceVisuals(); el('leadBox').textContent='READY TO RACE'; el('raceResult').classList.add('hidden'); el('countdown').textContent=''; el('playerTruck').classList.remove('launching','running','nitrous-on'); el('opponentTruck').classList.remove('launching','running');}
async function startRace(){resetRace(); race.active=true; const cd=el('countdown'); lightOn('pre1'); lightOn('pre2'); el('playerTruck').classList.add('launching'); el('opponentTruck').classList.add('launching'); await sleep(500); for(const step of [{text:'3',bulb:'amber1'},{text:'2',bulb:'amber2'},{text:'1',bulb:'amber3'}]){cd.textContent=step.text; lightOn(step.bulb); await sleep(650);} cd.textContent='GO!'; lightOn('greenBulb'); race.started=true; race.launchTime=performance.now(); race.lastTime=performance.now(); el('playerTruck').classList.remove('launching'); el('opponentTruck').classList.remove('launching'); el('playerTruck').classList.add('running'); el('opponentTruck').classList.add('running'); await sleep(450); cd.textContent='';}
el('startRaceBtn').addEventListener('click',startRace);
function shiftGear(){if(!race.started||race.finished) return; if(race.gear<6){const sweet=race.rpm>=4200&&race.rpm<=5200; race.gear++; race.rpm=sweet?2850:2300;}}
function setThrottle(on){race.throttle=on;} function setNitrous(on){race.nitrousOn=on&&race.nitrous>0&&race.started&&!race.finished; el('playerTruck').classList.toggle('nitrous-on',race.nitrousOn);} const throttleBtn=el('throttleBtn'); ['pointerdown','touchstart'].forEach(evt=>throttleBtn.addEventListener(evt,e=>{e.preventDefault();setThrottle(true);},{passive:false})); ['pointerup','pointercancel','pointerleave','touchend'].forEach(evt=>throttleBtn.addEventListener(evt,e=>{e.preventDefault();setThrottle(false);},{passive:false})); el('shiftBtn').addEventListener('pointerdown',shiftGear); const nitrousBtn=el('nitrousBtn'); ['pointerdown','touchstart'].forEach(evt=>nitrousBtn.addEventListener(evt,e=>{e.preventDefault();setNitrous(true);},{passive:false})); ['pointerup','pointercancel','pointerleave','touchend'].forEach(evt=>nitrousBtn.addEventListener(evt,e=>{e.preventDefault();setNitrous(false);},{passive:false})); window.addEventListener('keydown',e=>{if(['ArrowUp','w','W'].includes(e.key)) setThrottle(true); if(e.key==='Shift') shiftGear(); if(['n','N'].includes(e.key)) setNitrous(true);}); window.addEventListener('keyup',e=>{if(['ArrowUp','w','W'].includes(e.key)) setThrottle(false); if(['n','N'].includes(e.key)) setNitrous(false);});
function updateRace(dt){if(!race.started||race.finished) return; const powerFactor=state.stats.power/425; const gripFactor=Math.max(.75,state.stats.grip/62); const weightFactor=7150/state.stats.weight; const gearMax=[0,38,66,96,128,158,185]; const maxForGear=gearMax[race.gear]; const rpmTarget=900+(race.speed/maxForGear)*4400; if(race.throttle){let accel=(24*powerFactor*weightFactor*Math.min(gripFactor,1.35))/(1+(race.gear-1)*.18); if(race.rpm>5300) accel*=.42; if(race.rpm<1600&&race.gear>1) accel*=.55; if(race.nitrousOn&&race.nitrous>0){accel*=1.28; race.nitrous=Math.max(0,race.nitrous-22*dt);} race.speed+=accel*dt; race.rpm+=(rpmTarget-race.rpm)*Math.min(1,dt*7);} else {race.speed-=5.5*dt; race.rpm+=(900-race.rpm)*Math.min(1,dt*4);} race.speed=Math.max(0,Math.min(race.speed,maxForGear+9)); race.rpm=Math.max(850,Math.min(5900,race.rpm)); race.distance+=race.speed*1.46667*dt; const performanceBias=108+Math.random()*2.5; if((performance.now()-race.launchTime)<700){race.opponentSpeed+=(78-race.opponentSpeed)*dt*1.3;} else {race.opponentSpeed+=(performanceBias-race.opponentSpeed)*dt*.8;} race.opponentDistance+=Math.max(0,race.opponentSpeed)*1.46667*dt; if(race.distance>=1320||race.opponentDistance>=1320){race.finished=true; finishRace();}}
function updateRaceVisuals(){const playerTruck=el('playerTruck'); const opponentTruck=el('opponentTruck'); const scene=el('raceScene'); const finishX=scene.clientWidth*.90; const startX=scene.clientWidth*.07; const raceSpan=finishX-startX-playerTruck.offsetWidth*.35; const pProgress=Math.min(1,race.distance/1320); const oProgress=Math.min(1,race.opponentDistance/1320); const playerX=startX+raceSpan*pProgress; const oppX=startX+raceSpan*oProgress; const baseRaise=Number(playerTruck.dataset.raise||'0'); const baseScale=Number(playerTruck.dataset.scale||'1'); const squat=race.throttle&&race.started&&!race.finished?Math.min(9,race.speed*.06):0; playerTruck.style.transform=`translate(${playerX}px, ${-baseRaise+squat}px) scale(${baseScale})`; opponentTruck.style.transform=`translate(${oppX}px, 0px) scale(1)`; el('playerProgress').style.width=`${pProgress*100}%`; el('opponentProgress').style.width=`${oProgress*100}%`; const lead=race.distance-race.opponentDistance; if(!race.started){el('leadBox').textContent='READY TO RACE';} else if(Math.abs(lead)<8){el('leadBox').textContent='NECK AND NECK';} else if(lead>0){el('leadBox').textContent=`YOU LEAD BY ${Math.round(lead)} FT`; } else {el('leadBox').textContent=`RIVAL LEADS BY ${Math.round(Math.abs(lead))} FT`;}}
function finishRace(){if(race.resultShown) return; race.resultShown=true; race.nitrousOn=false; el('playerTruck').classList.remove('nitrous-on'); el('playerTruck').classList.remove('running'); el('opponentTruck').classList.remove('running'); const win=race.distance>=1320&&race.distance>=race.opponentDistance; const elapsed=((performance.now()-race.launchTime)/1000).toFixed(2); const payout=win?1800:250; state.money+=payout; localStorage.setItem('dl_money',state.money); el('money').textContent=state.money.toLocaleString(); const box=el('raceResult'); box.classList.remove('hidden'); box.textContent=win?`WIN — ${elapsed}s — Prize $${payout.toLocaleString()}`:`LOSS — ${elapsed}s — Participation $${payout.toLocaleString()}`;}
function updateHud(){el('speed').textContent=Math.round(race.speed); el('rpm').textContent=Math.round(race.rpm); el('gear').textContent=race.gear; el('distance').textContent=Math.min(1320,Math.round(race.distance)); el('nitrous').textContent=Math.round(race.nitrous);} function gameLoop(now){const dt=Math.min(.033,(now-(race.lastTime||now))/1000); race.lastTime=now; if(race.active) updateRace(dt); updateRaceVisuals(); updateHud(); requestAnimationFrame(gameLoop);} resetRace(); requestAnimationFrame(gameLoop);
